# Yahoo Finance: Price Data

Don't call this directly. The `get_company_snapshot` tool uses it internally to fetch current price, 52-week range, and daily closing price history.

Read-only. There are no write endpoints on this connection.
